- @listings.each do |listing|
	.col-xs-6.col-md-3.listing-item data-price="#{listing.price.to_i}"
		.col-xs-12.col-md-12.thumbnail
			h5.col-xs-12.no-pad= truncate(listing.title, length: 30).titleize
<<<<<<< HEAD
			= link_to listing_path(listing),class: "col-xs-12 no-pad"
				.image-container style="background-image: url('#{image_url("no_image.png")}')"
=======
			- if listing.images.present?
				= link_to image_tag(listing.images[0].image.url, class: "col-xs-12 no-pad"), listing_path(listing),class: "col-xs-12 no-pad"
			- else
				= link_to image_tag('no_pic.jpg', class: "col-xs-12 no-pad"), listing_path(listing),class: "col-xs-12 no-pad"
>>>>>>> 9dd9f6e03627deb928c79bd60a06b977a2189043
			.col-xs-6.no-pad
				.time-post.pull-left= time_ago_in_words(listing.created_at)
				ago
			.col-xs-6.no-pad
				.price.pull-right class="price-#{listing.price.to_i}" id="#{listing.price.to_i}"
					| $
					= listing.price.to_i